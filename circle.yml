machine:
  environment:
    GYM_CODE_SIGNING_IDENTITY: "iPhone Distribution: C. & J. CLARK (HOLDINGS) LTD/"
  xcode:
    version: "7.3"
dependencies:
  pre:
    - sudo gem install rubygems-update
test:
  override:
    - ./scripts/add-ke.sh
deployment:
 beta_distribution:
   branch: testioski
   commands:
     - gym
 #    - cp -r /Users/distiller/clarks-ios/AW17.ipa $CIRCLE_ARTIFACTS/
 #    - ./iparename.sh
 #    - Crashlytics.framework/submit $API_KEY $BUILD_SECRET -ipaPath /Users/distiller/clarks-ios/AW17.ipa -emails adarsh.r@openly.co -notesPath ReleaseNote/ReleaseNotes.txt
 # staging:
 #   branch: /feature-sprint.*/
 #   commands:
 #     - gym
 #     - cp -r /Users/distiller/clarks-ios/AW17.ipa $CIRCLE_ARTIFACTS/
 #     - ./iparename.sh
 #     - Crashlytics.framework/submit $API_KEY $BUILD_SECRET -ipaPath /Users/distiller/clarks-ios/AW17.ipa -emails 1409adarsh@gmail.com -notesPath ReleaseNote/ReleaseNotes.txt
 # release:
 #   tag: /release-.*/
 #   commands:
 #     - gym
 #     - cp -r /Users/distiller/clarks-ios/AW17.ipa $CIRCLE_ARTIFACTS/
 #     - ./iparename.sh
 #     - Crashlytics.framework/submit $API_KEY $BUILD_SECRET -ipaPath /Users/distiller/clarks-ios/AW17.ipa -emails 1409adarsh@gmail.com -notesPath ReleaseNote/ReleaseNotes.txt